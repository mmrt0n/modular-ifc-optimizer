# 모듈러 건축 IFC 최적화 시스템

> IFC 파일에서 벽체 정보를 자동 추출하여 석고보드·합판 시공 배치를 최적화하고,  
> 결과를 인터랙티브 시뮬레이터로 출력하는 자동화 도구입니다.

<br>

## ⬇️ 다운로드 (설치 없이 바로 실행)

| 파일 | 설명 |
|------|------|
| **[IFC최적화_v5.exe](dist/IFC최적화_v5.exe)** | Windows 64비트 실행 파일 (~54MB) |

> Python, Node.js 등 별도 설치 불필요 — EXE 하나로 실행 가능  
> Windows 10/11 64비트 / Chrome·Edge 브라우저 권장

<br>

## ✨ 주요 기능

| 기능 | 설명 |
|------|------|
| 🔍 IFC 자동 파싱 | IfcWall 추출, 치수·개구부 자동 인식 및 단위 보정 |
| 📐 다중 개구부 배치 | 벽당 문+창 여러 개를 각각 독립적으로 정확히 처리 |
| ⚡ 4조합 사전계산 | 석고보드 1P/2P · 합판 1P/2P 모두 사전계산, 시뮬레이터에서 실시간 전환 |
| ♻️ 재사용 로직 | 자투리 보드 자동 저장 → 같은 공간·층 내 재활용 (ReusePool) |
| 🖥️ 자체완결형 시뮬레이터 | 단계별 애니메이션, Layer 1/2·합판보강 전환, 재생 속도 조절 |
| 📊 자재 발주 일람표 | 벽별 수량 집계, 팔레트 단위 발주량 산출 |
| 🔎 QA 자동 전수조사 | 모든 벽 공백·겹침·타공누락·데이터오류 자동 감지 후 경보 |

<br>

## 🎨 시뮬레이터 색상 가이드

| 색상 | 종류 | 설명 |
|------|------|------|
| 진파랑 | 온장 Layer 1 | 규격 그대로 부착 (석고보드 900×1800 / 합판 1220×2440) |
| 진주황 | 온장 Layer 2 | 2P 시공 두 번째 레이어 |
| 연파랑 | 절단 Layer 1 | 벽 가장자리·개구부에 맞게 자른 보드 |
| 연주황 | 절단 Layer 2 | |
| 노랑 | 개구부 처리 (cut_op) | 창문 위·아래 구간 보드 |
| 초록 | ♻ 재사용 | 다른 벽 자투리를 재활용한 보드 |
| 회색 | 개구부 | 문·창 공간 |

<br>

## 🚀 사용 방법

1. `IFC최적화_v5.exe` 실행
2. **자투리 재사용** 여부 선택 (활성 권장)
3. **IFC 파일** 선택
4. (선택) 교차검증 기준값 입력
5. ▶ 실행 → 브라우저에서 결과 자동 오픈

> **자재(석고보드/합판)·공법(1P/2P)** 은 EXE 실행 시 선택하지 않습니다.  
> 시뮬레이터 HTML 안에서 버튼으로 언제든 전환 가능합니다.

**출력 파일 (IFC 파일과 같은 폴더에 저장)**

```
프로젝트_검증보고서.html              ← IFC 벽체 검증 리포트
프로젝트_석고보드_2P최적화결과.html   ← 배치도 + 자재 집계
프로젝트_시뮬레이터.html             ← 인터랙티브 시뮬레이터 ★
```

<br>

## 📦 자재 규격 및 팔레트

| 자재 | 규격 | 팔레트 수량 |
|------|------|-----------|
| 석고보드 | 900 × 1800mm | 990매/팔레트 |
| 합판 | 1220 × 2440mm | 100매/팔레트 |

<br>

## 🛠️ 개발 환경 설정 (소스 수정 시)

```bash
git clone https://github.com/mmrt0n/modular-ifc-optimizer.git
cd modular-ifc-optimizer
pip install ifcopenshell numpy pyinstaller
python ifc_pipeline.py
```

**EXE 재빌드**

```bash
python -m PyInstaller IFC최적화_v5.spec --clean --noconfirm
# 결과물: dist/IFC최적화_v5.exe
```

<br>

## 📁 파일 구조

```
modular-ifc-optimizer/
├── ifc_pipeline.py          # 전체 파이프라인 오케스트레이터 (Tkinter 마법사)
├── ifc_verifier.py          # IFC 파싱 및 벽체 검증
├── gypsum_optimizer_v3.py   # 배치 최적화 엔진 + HTML 생성 (시뮬레이터 포함)
├── IFC최적화_v5.spec        # PyInstaller 빌드 설정
├── dist/
│   └── IFC최적화_v5.exe     # 배포용 실행 파일
└── docs/
    ├── IFC_원본_오류_사항.md
    └── images/
```

<br>

## 📋 시공 로직 구현 현황 (M3 방식)

| 항목 | 구현 여부 | 비고 |
|------|-----------|------|
| RTL (오른쪽 끝부터) 배치 | ✅ | Python 사전계산 |
| 자투리 벽 가장자리 배치 | ✅ | 개구부 옆 작은조각 제거 |
| **다중 개구부 독립 처리** | ✅ | **문+창 혼합 벽 각각 정확 처리** |
| 각재 피치 450mm (STUD) | ✅ | 2P 레이어 엇갈림 |
| 바닥 밀착 (FLOOR_GAP=0) | ✅ | M3 확인결과 9-1 |
| 직선절단 / 개구부처리 분류 | ✅ | cut / cut_op |
| 재사용 풀 (MIN_W=300 / MIN_H=450) | ✅ | ReusePool |
| 4조합 동시 사전계산 | ✅ | gyp1/gyp2/ply1/ply2 |
| 합판 보강 영역 계산 | ✅ | 도어·TV·선반 보강 자동 산출 |
| QA 자동 전수조사 | ✅ | 공백·겹침·타공누락·코드중복 4종 |
| SVG 도면 스케일 보정 | ✅ | 좁은 벽 비율 보정 |
| **자체완결형 시뮬레이터** | ✅ | **템플릿 주입 방식 폐기, 단일 HTML 생성** |

<br>

## 🏗️ 시스템 아키텍처

```
IFC 파일
   │
   ▼
ifc_verifier.py  ──→  _검증보고서.html
   │
   ▼
gypsum_optimizer_v3.py
   ├─ 4조합 계산 (석고1P·2P / 합판1P·2P)
   ├─ _최적화결과.html  (SVG 배치도 + 절단 목록)
   └─ _시뮬레이터.html  (자체완결형 — 데이터 내장)
          ├─ 자재·공법 4조합 토글 (실시간 전환)
          ├─ Layer 1 / Layer 2 / 합판보강 전환
          ├─ 단계별 배치 애니메이션
          └─ QA 자동 전수조사 경보
```

<br>

## 📄 문서

- [IFC 원본 오류 사항](docs/IFC_원본_오류_사항.md) — IFC 파일 데이터 이상 보고서

<br>

---

인천대학교 건축환경연구실
